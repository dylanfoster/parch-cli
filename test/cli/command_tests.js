"use strict";

import { expect } from "chai";

describe("Command", function () {
  describe("projectName", function () {
    it("returns the base name of the current directory");
  });

  describe("name", function () {
    it("returns the lowercase command name");
  });
});
