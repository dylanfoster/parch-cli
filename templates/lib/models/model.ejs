"use strict";

class <%= upperCaseName %>Model extends Model {
  constructor() {
  }

  associate(<%= upperCaseName %>, models) {
  }

  define(dataTypes) {
    const <%= name %> = {
    };

    return <%= name %>
  }
}

module.exports = <%= upperCaseName %>;
