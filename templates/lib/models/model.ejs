"use strict";

const { Model } = require("parch");

class <%= upperCaseName %>Model extends Model {
  constructor() {
    super({
      hooks: {
        beforeCreate(/* instance */) {
          return;
        }
      },
      instanceMethods: {
      }
    });
  }

  associate(/* <%= upperCaseName %>, models */) {
  }

  define(/* dataTypes */) {
    const <%= name %> = {
    };

    return <%= name %>;
  }
}

module.exports = <%= upperCaseName %>Model;
