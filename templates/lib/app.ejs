"use strict";

const { Application } = require("parch");

const app = module.exports = new Application({
  authentication: {
  },
  controllers: {},
  database: {
  connection: {},
  logging: {
    dir: ""
  },
  models: {}
  },
  namespace: "api",
  server: {}
});

app.map(function () {
  this.resource("foo");
});
