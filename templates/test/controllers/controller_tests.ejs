"use strict";

const { expect } = require("chai");

describe("Controller | <%= upperCaseName %>", function () {
  describe("GET /api/<%= pluralName %>", function () {
    it("returns all <%= pluralName %>");
  });

  describe("GET /api/<%= pluralName %>/:<%= name %>Id", function () {
    it("returns a single <%= name %>");
  });

  describe("POST /api/<%= pluralName %>", function () {
    it("creates a <%= name %>");
  });

  describe("PUT /api/<%= pluralName %>", function () {
    it("updates a <%= name %>");
  });

  describe("DELETE /api/<%= pluralName %>", function () {
    it("destroys a <%= name %>");
  });
});
